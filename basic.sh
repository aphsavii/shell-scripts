#!/bin/bash    

#shebang line - this is the path to the bash interpreter, specifying the location of the bash interpreter. the first line is not a comment.

echo "Hello World!" # to display something in the terminal

# to run the script, type the following command in the terminal 
# ls -l basic.sh # to check the permissions of the script
# chmod +x basic.sh # change perminssion to make file executable 
# ./basic.sh # to run the script
